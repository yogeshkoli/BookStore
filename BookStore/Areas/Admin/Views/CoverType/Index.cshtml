@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "CoverType";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">
                Cover Type List
            </h2>
        </div>

        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create New Cover Type
            </a>
        </div>
    </div>
</div>

<div class="mt-3">
    <table class="table table-bordered table-striped" style="width: 100%;">
        <thead>
            <tr>
                <th>Cover Type Name</th>
               
                <th style="width: 20%;">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var coverType in Model)
            {
                <tr>
                    <td>
                        @coverType.Name
                    </td>
                    <td>
                        <div class="btn-group" role="group">
                            <a asp-controller="CoverType" class="btn btn-primary mx-2" asp-action="Edit" asp-route-id="@coverType.Id"><i class="bi bi-pencil-square"></i></a>
                            <a asp-controller="CoverType" class="btn btn-danger mx-2" asp-action="Delete" asp-route-id="@coverType.Id"><i class="bi bi-archive"></i></a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>  
</div>